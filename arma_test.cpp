#include "stdafx.h"
#include <iostream>
#include "arma_test.h"
#include <armadillo>
void Arma_Test::test()
{
	//std::cout << "test" << std::endl;
	//arma::mat A = arma::randu<arma::mat>(20000, 6000);
	////arma::mat B = arma::cov(A);
	////auto C = arma::eig_sym(B);
	//arma::mat coeff;
	//arma::mat score;
	//arma::vec latent;
	//arma::vec tsquared;
	//arma::princomp(coeff, score, latent, tsquared, A);
	//std::cout << latent << std::endl;
}